# Learnings (project-specific)

Distillate from retros at the milestone/MVP gate — max. 3–5 entries per retro, no running log.
Generalizable entries additionally go into the global knowledge base
(`$SKILLS_KNOWLEDGE_DIR` if set, otherwise the path in `<skills-dir>/_shared/knowledge.path`).

## L-001: <rule in one sentence>
- Why: <evidence — finding/blocker/ADR, cycle>
- Application: <who, when>
